{
  "name": "gulp-overview",
  "version": "1.0.0",
  "description": "gulp overview example",
  "main": "builds/js/main.js",
  "private": true,
  "dependencies": {},
  "devDependencies": {
    "gulp": "^3.9.1",
    "gulp-clean": "^0.3.2",
    "gulp-concat": "^2.6.0",
    "http-server": "^0.9.0"
  },
  "scripts": {
    "openInBrowser": "start http://localhost:8000/builds ",
    "server": "http-server -a localhost -p 8000",
    "start:prod": "SET NODE_ENV=production && gulp",
    "start:dev": "SET NODE_ENV=development && gulp",
    "gulp": "SET NODE_ENV=production && gulp"
  },
  "author": "Sarhan Azizov",
  "license": "ISC"
}
